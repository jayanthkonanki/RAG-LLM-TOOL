import type { Application, EndpointGroup, Endpoint, FileNode } from '@/types';

export const MOCK_APPLICATIONS: Application[] = [
  {
    id: 'app-1',
    name: 'User Service',
    description: 'Core user identity, authentication and profile management service.',
    tags: ['identity', 'auth', 'core'],
    groupIds: ['grp-1', 'grp-2'],
    createdAt: '2024-01-10T09:00:00Z',
    updatedAt: '2024-03-15T14:30:00Z',
  },
  {
    id: 'app-2',
    name: 'Billing Service',
    description: 'Subscription, invoicing, and payment processing service.',
    tags: ['billing', 'payments', 'stripe'],
    groupIds: ['grp-3'],
    createdAt: '2024-02-01T09:00:00Z',
    updatedAt: '2024-03-20T10:00:00Z',
  },
  {
    id: 'app-3',
    name: 'Analytics Service',
    description: 'Event tracking, metrics aggregation, and reporting APIs.',
    tags: ['analytics', 'metrics', 'reporting'],
    groupIds: ['grp-4'],
    createdAt: '2024-02-15T09:00:00Z',
    updatedAt: '2024-03-22T12:00:00Z',
  },
];

export const MOCK_GROUPS: EndpointGroup[] = [
  { id: 'grp-1', applicationId: 'app-1', name: 'Auth APIs', description: 'Authentication and session management', endpointIds: ['ep-1', 'ep-2'], createdAt: '2024-01-10T09:00:00Z' },
  { id: 'grp-2', applicationId: 'app-1', name: 'Profile APIs', description: 'User profile CRUD operations', endpointIds: ['ep-3'], createdAt: '2024-01-12T09:00:00Z' },
  { id: 'grp-3', applicationId: 'app-2', name: 'Payment APIs', description: 'Charge, refund and subscription endpoints', endpointIds: ['ep-4', 'ep-5'], createdAt: '2024-02-01T09:00:00Z' },
  { id: 'grp-4', applicationId: 'app-3', name: 'Events APIs', description: 'Event ingestion and query endpoints', endpointIds: ['ep-6'], createdAt: '2024-02-15T09:00:00Z' },
];

export const MOCK_ENDPOINTS: Endpoint[] = [
  {
    id: 'ep-1',
    groupId: 'grp-1',
    applicationId: 'app-1',
    name: 'User Login',
    method: 'POST',
    path: '/auth/login',
    shortDescription: 'Authenticate a user and return a JWT access token.',
    longDescription: 'Accepts email and password credentials. Validates against the user store, checks account status, and returns a signed JWT with a 1-hour expiry alongside a refresh token. Triggers login audit log entry.',
    category: 'authentication',
    tags: ['auth', 'jwt', 'session', 'core'],
    riskLevel: 'medium',
    aiReadabilityScore: 91,
    createdAt: '2024-01-10T09:00:00Z',
    updatedAt: '2024-03-15T14:30:00Z',
    requestSchema: {
      headers: [{ name: 'Content-Type', type: 'string', required: true, description: 'Must be application/json', example: 'application/json' }],
      queryParams: [],
      bodySchema: { email: 'string', password: 'string' },
      requiredFields: ['email', 'password'],
      optionalFields: ['rememberMe'],
      validationNotes: 'Email must be valid RFC 5322 format. Password minimum 8 chars.',
    },
    responseSchema: {
      successSchema: { accessToken: 'string', refreshToken: 'string', expiresIn: 'number', user: { id: 'string', email: 'string', role: 'string' } },
      successExample: { accessToken: 'eyJhbGci...', refreshToken: 'dGhpcyBpcyBh...', expiresIn: 3600, user: { id: 'u-123', email: 'user@example.com', role: 'member' } },
      failureExamples: [
        { status: 401, body: { error: 'INVALID_CREDENTIALS', message: 'Email or password is incorrect' }, description: 'Wrong credentials' },
        { status: 403, body: { error: 'ACCOUNT_LOCKED', message: 'Account locked after 5 failed attempts' }, description: 'Brute force lockout' },
      ],
      commonErrors: ['401 Invalid credentials', '403 Account locked', '429 Rate limited'],
      edgeCases: ['SSO users cannot use password login', 'Deleted accounts return 404 not 401'],
    },
    agentContext: {
      toolName: 'user_login',
      naturalLanguageDescription: 'Log in a user with email and password and retrieve a JWT access token for subsequent API calls.',
      usageGuidance: 'Call this endpoint first before any authenticated endpoint. Store the accessToken and include it in Authorization headers.',
      executionExamples: ['user_login(email="alice@example.com", password="secret123")'],
      failureScenarios: ['User not found', 'Wrong password', 'Account locked', 'Rate limit exceeded'],
      operationalWarnings: ['Do not retry more than 3 times — account will be locked', 'Token expires in 1 hour'],
      semanticTags: ['authenticate', 'session-start', 'credential-exchange'],
      businessDomain: 'Identity & Access',
      recommendedExecutionOrder: 1,
      whenToUse: 'When the agent needs to act on behalf of an authenticated user.',
      whenNotToUse: 'When a valid token already exists in context. Do not call if user is already authenticated.',
      existingRisks: ['Account lockout on repeated failure', 'JWT exposure in logs'],
      existingDependencies: ['User must exist in the system', 'Account must not be suspended'],
    },
    agentContextLabels: {
      businessPurpose: 'Grant access to authenticated user sessions across all services.',
      executionIntent: 'Exchange credentials for a scoped JWT token.',
      expectedOutcome: 'Returns a valid access token usable for 1 hour.',
      requiredConditions: 'User account must be active and not locked.',
      unsafeOperationsWarning: 'Repeated calls with wrong password lock the account.',
      sideEffects: 'Creates an audit log entry. Updates last-login timestamp.',
      dependencies: ['UserStore', 'AuditLog', 'TokenService'],
      requiredAuthentication: 'None — this is the auth entry point.',
      dataSensitivity: 'confidential',
      rateLimitAwareness: '10 requests per minute per IP.',
      idempotent: false,
      retrySafe: false,
    },
    notes: 'Consider adding MFA step-up for high-privilege accounts. Discuss with security team Q3.',
  },
  {
    id: 'ep-2',
    groupId: 'grp-1',
    applicationId: 'app-1',
    name: 'Register User',
    method: 'POST',
    path: '/auth/register',
    shortDescription: 'Create a new user account.',
    longDescription: 'Creates a new user record, sends a verification email, and returns the created user object. Does not grant access until email is verified.',
    category: 'authentication',
    tags: ['auth', 'signup', 'onboarding'],
    riskLevel: 'low',
    aiReadabilityScore: 84,
    createdAt: '2024-01-11T09:00:00Z',
    updatedAt: '2024-03-10T12:00:00Z',
    requestSchema: {
      headers: [{ name: 'Content-Type', type: 'string', required: true, description: 'application/json', example: 'application/json' }],
      queryParams: [],
      bodySchema: { email: 'string', password: 'string', name: 'string' },
      requiredFields: ['email', 'password', 'name'],
      optionalFields: ['inviteCode'],
      validationNotes: 'Password must be 8+ chars with at least one number.',
    },
    responseSchema: {
      successSchema: { user: { id: 'string', email: 'string', name: 'string', verified: 'boolean' } },
      successExample: { user: { id: 'u-456', email: 'new@example.com', name: 'Jane Doe', verified: false } },
      failureExamples: [{ status: 409, body: { error: 'EMAIL_EXISTS' }, description: 'Duplicate email' }],
      commonErrors: ['409 Email already registered', '422 Validation error'],
      edgeCases: ['Invite-only mode returns 403 without valid inviteCode'],
    },
    agentContext: {
      toolName: 'register_user',
      naturalLanguageDescription: 'Create a new user account in the system.',
      usageGuidance: 'Call only when creating a brand new user. Follow with email verification flow.',
      executionExamples: ['register_user(email="new@example.com", password="Pass1234", name="Jane Doe")'],
      failureScenarios: ['Duplicate email', 'Weak password', 'Invite code required'],
      operationalWarnings: ['This creates a permanent record. Cannot be undone without admin intervention.'],
      semanticTags: ['create-user', 'signup', 'onboarding'],
      businessDomain: 'Identity & Access',
      recommendedExecutionOrder: null,
      whenToUse: 'When onboarding a new user who does not exist.',
      whenNotToUse: 'When the user already has an account.',
      existingRisks: ['Spam account creation', 'Email enumeration'],
      existingDependencies: ['EmailService for verification'],
    },
    agentContextLabels: {
      businessPurpose: 'Onboard new users into the platform.',
      executionIntent: 'Create a verified user identity.',
      expectedOutcome: 'User record created; verification email sent.',
      requiredConditions: 'Email must not already be registered.',
      unsafeOperationsWarning: 'Creates permanent DB record.',
      sideEffects: 'Sends verification email. Charges may apply for email service.',
      dependencies: ['EmailService', 'UserStore'],
      requiredAuthentication: 'None',
      dataSensitivity: 'confidential',
      rateLimitAwareness: '5 registrations per hour per IP.',
      idempotent: false,
      retrySafe: false,
    },
    notes: '',
  },
  {
    id: 'ep-3',
    groupId: 'grp-2',
    applicationId: 'app-1',
    name: 'Get Profile',
    method: 'GET',
    path: '/users/{userId}/profile',
    shortDescription: 'Fetch a user profile by ID.',
    longDescription: 'Returns the full user profile including name, avatar, preferences, and account metadata. Requires bearer token.',
    category: 'data-retrieval',
    tags: ['profile', 'user', 'read'],
    riskLevel: 'safe',
    aiReadabilityScore: 76,
    createdAt: '2024-01-15T09:00:00Z',
    updatedAt: '2024-02-20T10:00:00Z',
    requestSchema: {
      headers: [{ name: 'Authorization', type: 'string', required: true, description: 'Bearer JWT token', example: 'Bearer eyJhbGci...' }],
      queryParams: [{ name: 'include', type: 'string', required: false, description: 'Comma-separated fields to include', example: 'preferences,avatar' }],
      bodySchema: {},
      requiredFields: [],
      optionalFields: ['include'],
      validationNotes: 'userId must be a valid UUID.',
    },
    responseSchema: {
      successSchema: { id: 'string', name: 'string', email: 'string', avatar: 'string|null', preferences: 'object', createdAt: 'string' },
      successExample: { id: 'u-123', name: 'Alice Smith', email: 'alice@example.com', avatar: 'https://cdn.example.com/avatar.jpg', preferences: { theme: 'dark' }, createdAt: '2024-01-01T00:00:00Z' },
      failureExamples: [{ status: 404, body: { error: 'USER_NOT_FOUND' }, description: 'User does not exist' }],
      commonErrors: ['401 Unauthorized', '404 User not found'],
      edgeCases: ['Deleted users return 404', 'Self-access vs admin-access returns different field sets'],
    },
    agentContext: {
      toolName: 'get_user_profile',
      naturalLanguageDescription: 'Retrieve a user profile object by their unique user ID.',
      usageGuidance: 'Use after authentication to fetch current user context. Pass userId from login response.',
      executionExamples: ['get_user_profile(userId="u-123")'],
      failureScenarios: ['Invalid userId format', 'User deleted', 'Token expired'],
      operationalWarnings: ['Do not expose full profile in logs — contains PII.'],
      semanticTags: ['read-user', 'profile', 'identity-context'],
      businessDomain: 'Identity & Access',
      recommendedExecutionOrder: 2,
      whenToUse: 'When the agent needs user context like name, preferences, or role.',
      whenNotToUse: 'When userId is not known — resolve it first via user_login.',
      existingRisks: ['PII exposure in logs'],
      existingDependencies: ['Valid JWT token', 'User must exist'],
    },
    agentContextLabels: {
      businessPurpose: 'Provide agent with full user context for personalized operations.',
      executionIntent: 'Read and return a user\'s profile data.',
      expectedOutcome: 'Returns profile object with name, email, preferences.',
      requiredConditions: 'Valid Bearer token. User must exist.',
      unsafeOperationsWarning: 'None — read-only.',
      sideEffects: 'None.',
      dependencies: ['JWT Auth', 'UserStore'],
      requiredAuthentication: 'Bearer JWT',
      dataSensitivity: 'confidential',
      rateLimitAwareness: '100 requests per minute.',
      idempotent: true,
      retrySafe: true,
    },
    notes: 'Cache this response for 5 minutes to reduce DB load.',
  },
  {
    id: 'ep-4',
    groupId: 'grp-3',
    applicationId: 'app-2',
    name: 'Create Charge',
    method: 'POST',
    path: '/billing/charges',
    shortDescription: 'Create a new payment charge for a customer.',
    longDescription: 'Initiates a payment against a stored payment method. Integrates with Stripe. Returns charge object with status.',
    category: 'data-mutation',
    tags: ['billing', 'payment', 'stripe', 'critical'],
    riskLevel: 'critical',
    aiReadabilityScore: 88,
    createdAt: '2024-02-01T09:00:00Z',
    updatedAt: '2024-03-18T16:00:00Z',
    requestSchema: {
      headers: [
        { name: 'Authorization', type: 'string', required: true, description: 'Bearer JWT', example: 'Bearer eyJ...' },
        { name: 'Idempotency-Key', type: 'string', required: true, description: 'Unique key to prevent duplicate charges', example: 'idem-uuid-123' },
      ],
      queryParams: [],
      bodySchema: { customerId: 'string', amount: 'number', currency: 'string', paymentMethodId: 'string', description: 'string' },
      requiredFields: ['customerId', 'amount', 'currency', 'paymentMethodId'],
      optionalFields: ['description', 'metadata'],
      validationNotes: 'Amount in smallest currency unit (cents). Currency must be ISO 4217.',
    },
    responseSchema: {
      successSchema: { chargeId: 'string', status: 'string', amount: 'number', currency: 'string', createdAt: 'string' },
      successExample: { chargeId: 'ch-789', status: 'succeeded', amount: 4999, currency: 'usd', createdAt: '2024-03-18T16:00:00Z' },
      failureExamples: [
        { status: 402, body: { error: 'CARD_DECLINED', message: 'Insufficient funds' }, description: 'Card decline' },
        { status: 409, body: { error: 'DUPLICATE_CHARGE' }, description: 'Idempotency key reused with different params' },
      ],
      commonErrors: ['402 Card declined', '409 Duplicate charge'],
      edgeCases: ['Partial authorization (amount reduced by bank)', 'FX conversion charges'],
    },
    agentContext: {
      toolName: 'create_charge',
      naturalLanguageDescription: 'Charge a customer a specific amount in a given currency using their stored payment method.',
      usageGuidance: 'Always include Idempotency-Key. Verify customer payment method exists before calling. Do not retry without a new idempotency key.',
      executionExamples: ['create_charge(customerId="cus-123", amount=4999, currency="usd", paymentMethodId="pm-456", idempotencyKey="unique-key-abc")'],
      failureScenarios: ['Card declined', 'Invalid payment method', 'Currency mismatch', 'Fraud block'],
      operationalWarnings: ['CRITICAL: This causes an IRREVERSIBLE financial transaction.', 'Always use a unique Idempotency-Key.', 'Never retry with same idempotency key.'],
      semanticTags: ['payment', 'charge', 'financial-mutation', 'stripe'],
      businessDomain: 'Billing & Payments',
      recommendedExecutionOrder: null,
      whenToUse: 'When a user initiates a purchase or subscription payment.',
      whenNotToUse: 'Do not call unless user has explicitly confirmed the charge. Do not call in test mode with real keys.',
      existingRisks: ['Double charge if idempotency key not used', 'PCI compliance scope'],
      existingDependencies: ['Stripe integration', 'Customer must have a valid payment method', 'Currency must match account config'],
    },
    agentContextLabels: {
      businessPurpose: 'Collect payment from a customer for a product or service.',
      executionIntent: 'Execute a financial debit against a stored payment instrument.',
      expectedOutcome: 'Charge created with succeeded status and chargeId returned.',
      requiredConditions: 'Customer must have an active payment method. Amount must be positive.',
      unsafeOperationsWarning: '⚠ CRITICAL: Creates irreversible financial transaction. Requires explicit user confirmation.',
      sideEffects: 'Deducts funds from customer card. Creates invoice record. Sends payment receipt email.',
      dependencies: ['StripeAPI', 'CustomerStore', 'InvoiceService', 'EmailService'],
      requiredAuthentication: 'Bearer JWT + Admin or Billing scope',
      dataSensitivity: 'restricted',
      rateLimitAwareness: '100 charges per hour per account.',
      idempotent: false,
      retrySafe: false,
    },
    notes: 'Requires finance team sign-off for amounts > $10,000.',
  },
  {
    id: 'ep-5',
    groupId: 'grp-3',
    applicationId: 'app-2',
    name: 'Get Invoice',
    method: 'GET',
    path: '/billing/invoices/{invoiceId}',
    shortDescription: 'Retrieve a specific invoice by ID.',
    longDescription: 'Returns full invoice details including line items, payment status, due date, and PDF download URL.',
    category: 'data-retrieval',
    tags: ['billing', 'invoice', 'read'],
    riskLevel: 'safe',
    aiReadabilityScore: 72,
    createdAt: '2024-02-05T09:00:00Z',
    updatedAt: '2024-02-28T10:00:00Z',
    requestSchema: {
      headers: [{ name: 'Authorization', type: 'string', required: true, description: 'Bearer JWT', example: 'Bearer eyJ...' }],
      queryParams: [],
      bodySchema: {},
      requiredFields: [],
      optionalFields: [],
      validationNotes: 'invoiceId must be prefixed with "inv-".',
    },
    responseSchema: {
      successSchema: { invoiceId: 'string', status: 'string', amount: 'number', dueDate: 'string', pdfUrl: 'string', lineItems: 'array' },
      successExample: { invoiceId: 'inv-001', status: 'paid', amount: 4999, dueDate: '2024-04-01', pdfUrl: 'https://cdn.example.com/inv-001.pdf', lineItems: [] },
      failureExamples: [{ status: 404, body: { error: 'INVOICE_NOT_FOUND' }, description: 'Invoice not found' }],
      commonErrors: ['404 Invoice not found', '403 Access denied'],
      edgeCases: ['Voided invoices return status=voided but still exist'],
    },
    agentContext: {
      toolName: 'get_invoice',
      naturalLanguageDescription: 'Fetch details for a specific invoice by its ID.',
      usageGuidance: 'Use to retrieve payment status or share invoice PDF with user.',
      executionExamples: ['get_invoice(invoiceId="inv-001")'],
      failureScenarios: ['Invoice not found', 'Unauthorized access'],
      operationalWarnings: ['PDF URL expires after 24 hours.'],
      semanticTags: ['read-invoice', 'billing', 'financial-read'],
      businessDomain: 'Billing & Payments',
      recommendedExecutionOrder: null,
      whenToUse: 'When user asks about a specific invoice or payment.',
      whenNotToUse: 'When you need a list of invoices — use list_invoices instead.',
      existingRisks: ['Expired PDF links'],
      existingDependencies: ['Valid JWT', 'Invoice must exist'],
    },
    agentContextLabels: {
      businessPurpose: 'Provide invoice details to customers and finance systems.',
      executionIntent: 'Read and return invoice data.',
      expectedOutcome: 'Returns full invoice object with line items and PDF URL.',
      requiredConditions: 'Invoice ID must exist and belong to the authenticated user.',
      unsafeOperationsWarning: 'None — read-only.',
      sideEffects: 'None.',
      dependencies: ['InvoiceStore', 'PDF CDN'],
      requiredAuthentication: 'Bearer JWT',
      dataSensitivity: 'confidential',
      rateLimitAwareness: '200 requests per minute.',
      idempotent: true,
      retrySafe: true,
    },
    notes: '',
  },
  {
    id: 'ep-6',
    groupId: 'grp-4',
    applicationId: 'app-3',
    name: 'Track Event',
    method: 'POST',
    path: '/analytics/events',
    shortDescription: 'Ingest a single analytics event.',
    longDescription: 'Records a named event with properties into the analytics pipeline. Used for product analytics, funnel tracking, and A/B experiments.',
    category: 'analytics',
    tags: ['analytics', 'events', 'tracking'],
    riskLevel: 'low',
    aiReadabilityScore: 65,
    createdAt: '2024-02-15T09:00:00Z',
    updatedAt: '2024-03-01T10:00:00Z',
    requestSchema: {
      headers: [
        { name: 'X-API-Key', type: 'string', required: true, description: 'Analytics write key', example: 'ak-live-abc123' },
      ],
      queryParams: [],
      bodySchema: { event: 'string', userId: 'string', properties: 'object', timestamp: 'string' },
      requiredFields: ['event', 'userId'],
      optionalFields: ['properties', 'timestamp'],
      validationNotes: 'Event name must be snake_case. Timestamp defaults to server time if omitted.',
    },
    responseSchema: {
      successSchema: { received: 'boolean', eventId: 'string' },
      successExample: { received: true, eventId: 'evt-xyz-999' },
      failureExamples: [{ status: 400, body: { error: 'INVALID_EVENT_NAME' }, description: 'Event name validation failure' }],
      commonErrors: ['400 Invalid event name', '401 Invalid API key'],
      edgeCases: ['Events > 30 days old may be dropped by the pipeline'],
    },
    agentContext: {
      toolName: 'track_event',
      naturalLanguageDescription: 'Record a user action or system event in the analytics pipeline.',
      usageGuidance: 'Fire and forget — do not block on response. Call after significant user actions.',
      executionExamples: ['track_event(event="checkout_completed", userId="u-123", properties={"plan": "pro"})'],
      failureScenarios: ['Invalid API key', 'Malformed event name'],
      operationalWarnings: ['Do not track PII in event properties.', 'High-volume callers should use batch endpoint.'],
      semanticTags: ['event-ingestion', 'analytics', 'tracking'],
      businessDomain: 'Analytics & Data',
      recommendedExecutionOrder: null,
      whenToUse: 'After significant user actions to record behavior for product analytics.',
      whenNotToUse: 'Do not use for transactional data — use dedicated stores instead.',
      existingRisks: ['PII leakage in event properties'],
      existingDependencies: ['Analytics write key', 'Analytics pipeline availability'],
    },
    agentContextLabels: {
      businessPurpose: 'Feed user behavior into product analytics systems.',
      executionIntent: 'Write an event record to the analytics pipeline.',
      expectedOutcome: 'Event queued and acknowledged.',
      requiredConditions: 'Valid analytics write key.',
      unsafeOperationsWarning: 'Do not include PII in event properties.',
      sideEffects: 'Creates permanent event record in analytics warehouse.',
      dependencies: ['AnalyticsPipeline', 'EventQueue'],
      requiredAuthentication: 'X-API-Key header',
      dataSensitivity: 'internal',
      rateLimitAwareness: '1000 events per second per key.',
      idempotent: false,
      retrySafe: true,
    },
    notes: 'Consider switching to batch ingestion for agents that fire many events.',
  },
];

export const MOCK_FILE_TREE: FileNode[] = [
  {
    id: 'f-app-1', name: 'user-service', type: 'folder', children: [
      { id: 'f-1', name: 'endpoints.json', type: 'file', extension: 'json', size: '12 KB' },
      { id: 'f-2', name: 'schemas', type: 'folder', children: [
        { id: 'f-2a', name: 'login.schema.json', type: 'file', extension: 'json', size: '2 KB' },
        { id: 'f-2b', name: 'register.schema.json', type: 'file', extension: 'json', size: '1.8 KB' },
      ]},
      { id: 'f-3', name: 'examples', type: 'folder', children: [
        { id: 'f-3a', name: 'login.example.json', type: 'file', extension: 'json', size: '0.8 KB' },
      ]},
      { id: 'f-4', name: 'prompts', type: 'folder', children: [
        { id: 'f-4a', name: 'auth.agent.prompt.md', type: 'file', extension: 'md', size: '3 KB' },
      ]},
      { id: 'f-5', name: 'metadata', type: 'folder', children: [
        { id: 'f-5a', name: 'service.meta.json', type: 'file', extension: 'json', size: '0.5 KB' },
      ]},
      { id: 'f-6', name: 'notes.md', type: 'file', extension: 'md', size: '1.2 KB' },
    ],
  },
  {
    id: 'f-app-2', name: 'billing-service', type: 'folder', children: [
      { id: 'f-7', name: 'endpoints.json', type: 'file', extension: 'json', size: '8 KB' },
      { id: 'f-8', name: 'schemas', type: 'folder', children: [
        { id: 'f-8a', name: 'charge.schema.json', type: 'file', extension: 'json', size: '3 KB' },
      ]},
    ],
  },
];
